version: "3.8"
services:
  redis:
    image: redis:7-alpine
    container_name: timespace-redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    # (선택) 데이터 영속화:
    # volumes:
    #   - ./redis-data:/data
